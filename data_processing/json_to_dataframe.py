"""
This module contains modules related to processing JSON responses and creating Pandas DataFrames
"""

import pandas as pd


def json_to_dataframe(json_data):
    """
    Inputs JSON dictionary and returns a pandas DataFrame.
    """
    ### Convert JSON to Pandas Dataframe customised to JSON structure
    #
    return pd.DataFrame(json_data)
